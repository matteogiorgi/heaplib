#include <stddef.h>

#include "heaps/kaplan_heap.h"

/*
 * Stub for the planned Kaplan-heap-backed priority queue.
 */
struct priority_queue *kaplan_heap_create(priority_queue_cmp_fn cmp)
{
    (void) cmp;
    return NULL;
}
